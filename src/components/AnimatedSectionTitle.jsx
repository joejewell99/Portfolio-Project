import AnimatedText from './AnimatedText'

function AnimatedSectionTitle({ text }) {
  return <AnimatedText as="h2" text={text} className="animated-section-title" />
}

export default AnimatedSectionTitle
